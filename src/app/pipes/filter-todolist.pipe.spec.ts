import { FilterTodolistPipe } from './filter-todolist.pipe';

describe('FilterTodolistPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTodolistPipe();
    expect(pipe).toBeTruthy();
  });
});
